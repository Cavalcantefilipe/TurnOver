import { Button, Table } from "antd";
import styled from "styled-components";

const DivOperation = styled.div`
margin-top:15px;
  display: flex;
  align-items: center;
  align-self: center;
  flex-wrap: wrap;
  justify-content: space-between;
  background:white;
`;

const TableAll = styled(Table)`
  text-align:center;
  align-items: center;
  align-self:center;
  flex:auto;
  background:white;
`;

export { DivOperation, TableAll, };
